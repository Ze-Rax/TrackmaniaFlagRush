/**
 * Constants are used for Commands from other sources, like UI Events.
 * Constants value has to be the same as the name of the respective command without the Command_ prefix.
 * The Parameter for the Command has to be sent with the Event in the Params array.
 * In case of Vec2/Vec3/Int2/Int3 commands, X, Y & Z each take a seperate entry in the params array.
 */

// MARK: Progression
#Const Command_ResetFlag "ResetFlag"
#Const Command_Pause "PauseRound"
#Const Command_SkipRound "SkipRound"
#Const Command_EndWarmup "EndWarmup"
#Const Command_ApproveWarmUpEnd "ApproveWarmUpEnd"
#Const Command_SkipMap "SkipMap"
#Const Command_SetTimeLeft "SetTimeLeft"

// MARK: Scores
#Const Command_SetTeam1RoundScore "SetTeam1RoundScore"
#Const Command_SetTeam2RoundScore "SetTeam2RoundScore"
#Const Command_SetTeam1MapScore "SetTeam1MapScore"
#Const Command_SetTeam2MapScore "SetTeam2MapScore"
#Const Command_SetTeam1MatchScore "SetTeam1MatchScore"
#Const Command_SetTeam2MatchScore "SetTeam2MatchScore"
#Const Command_ClearScores "ClearScores"

// Other
#Const Command_Announce "Announce"
#Const Command_BalanceTeams "BalanceTeams"
#Const Command_SetBots "SetBots"
